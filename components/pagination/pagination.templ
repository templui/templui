package pagination

import (
	"github.com/axadrn/shadcn-templ/v2/components/button"
	"github.com/axadrn/shadcn-templ/v2/components/icon"
	"github.com/axadrn/shadcn-templ/v2/utils"
)

type Props struct {
	ID         string
	Class      string
	Attributes templ.Attributes
}

type ContentProps struct {
	ID         string
	Class      string
	Attributes templ.Attributes
}

type ItemProps struct {
	ID         string
	Class      string
	Attributes templ.Attributes
}

type LinkProps struct {
	ID         string
	Class      string
	Attributes templ.Attributes
	Href       string
	IsActive   bool
	Disabled   bool
}

type PreviousProps struct {
	ID         string
	Class      string
	Attributes templ.Attributes
	Href       string
	Disabled   bool
	Text      string
}

type NextProps struct {
	ID         string
	Class      string
	Attributes templ.Attributes
	Href       string
	Disabled   bool
	Text      string
}

templ Pagination(props ...Props) {
	{{ var p Props }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	<nav
		if p.ID != "" {
			id={ p.ID }
		}
		role="navigation"
		aria-label="pagination"
		data-slot="pagination"
		class={ utils.CN("cn-pagination mx-auto flex w-full justify-center", p.Class) }
		{ p.Attributes... }
	>
		{ children... }
	</nav>
}

templ Content(props ...ContentProps) {
	{{ var p ContentProps }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	<ul
		if p.ID != "" {
			id={ p.ID }
		}
		data-slot="pagination-content"
		class={ utils.CN("cn-pagination-content flex items-center", p.Class) }
		{ p.Attributes... }
	>
		{ children... }
	</ul>
}

templ Item(props ...ItemProps) {
	{{ var p ItemProps }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	<li
		if p.ID != "" {
			id={ p.ID }
		}
		data-slot="pagination-item"
		if p.Class != "" {
			class={ p.Class }
		}
		{ p.Attributes... }
	>
		{ children... }
	</li>
}

templ Link(props ...LinkProps) {
	{{ var p LinkProps }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	if p.Disabled {
		@button.Button(button.Props{
			ID:         p.ID,
			Disabled:   true,
			Size:       button.SizeIcon,
			Variant:    button.VariantGhost,
			Class:      utils.CN("cn-pagination-link", p.Class),
			Attributes: p.Attributes,
		}) {
			{ children... }
		}
	} else {
		{{
			attrs := templ.Attributes{"data-slot": "pagination-link"}
			if p.IsActive {
				attrs["aria-current"] = "page"
				attrs["data-active"] = "true"
			}
			for k, v := range p.Attributes {
				attrs[k] = v
			}
		}}
		@button.Button(button.Props{
			ID:         p.ID,
			Href:       p.Href,
			Size:       button.SizeIcon,
			Variant:    button.Variant(buttonVariant(p.IsActive)),
			Class:      utils.CN("cn-pagination-link", p.Class),
			Attributes: attrs,
		}) {
			{ children... }
		}
	}
}

templ Previous(props ...PreviousProps) {
	{{ var p PreviousProps }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	if p.Text == "" {
		{{ p.Text = "Previous" }}
	}
	{{
		attrs := templ.Attributes{"aria-label": "Go to previous page", "data-slot": "pagination-link"}
		for k, v := range p.Attributes {
			attrs[k] = v
		}
	}}
	@button.Button(button.Props{
		ID:         p.ID,
		Href:       p.Href,
		Disabled:   p.Disabled,
		Variant:    button.VariantGhost,
		Class:      utils.CN("cn-pagination-previous", p.Class),
		Attributes: attrs,
	}) {
		@icon.ChevronLeft(icon.Props{Class: "cn-rtl-flip", Attributes: templ.Attributes{"data-icon": "inline-start"}})
		<span class="cn-pagination-previous-text hidden sm:block">{ p.Text }</span>
	}
}

templ Next(props ...NextProps) {
	{{ var p NextProps }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	if p.Text == "" {
		{{ p.Text = "Next" }}
	}
	{{
		attrs := templ.Attributes{"aria-label": "Go to next page", "data-slot": "pagination-link"}
		for k, v := range p.Attributes {
			attrs[k] = v
		}
	}}
	@button.Button(button.Props{
		ID:         p.ID,
		Href:       p.Href,
		Disabled:   p.Disabled,
		Variant:    button.VariantGhost,
		Class:      utils.CN("cn-pagination-next", p.Class),
		Attributes: attrs,
	}) {
		<span class="cn-pagination-next-text hidden sm:block">{ p.Text }</span>
		@icon.ChevronRight(icon.Props{Class: "cn-rtl-flip", Attributes: templ.Attributes{"data-icon": "inline-end"}})
	}
}

templ Ellipsis() {
	<span aria-hidden="true" data-slot="pagination-ellipsis" class="cn-pagination-ellipsis flex items-center justify-center">
		@icon.Ellipsis()
		<span class="sr-only">More pages</span>
	</span>
}

func buttonVariant(isActive bool) button.Variant {
	if isActive {
		return button.VariantOutline
	}
	return button.VariantGhost
}
