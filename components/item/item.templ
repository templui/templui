package item

import "github.com/templui/templui/utils"

type Variant string
type Size string

const (
	VariantDefault Variant = "default"
	VariantOutline Variant = "outline"
	VariantMuted   Variant = "muted"
)

const (
	SizeDefault Size = "default"
	SizeSm      Size = "sm"
	SizeXs      Size = "xs"
)

type MediaVariant string

const (
	MediaVariantDefault MediaVariant = "default"
	MediaVariantIcon    MediaVariant = "icon"
	MediaVariantImage   MediaVariant = "image"
)

type Props struct {
	ID         string
	Class      string
	Attributes templ.Attributes
	Variant    Variant
	Size       Size
	Href       string
	Target     string
}

type GroupProps struct {
	ID         string
	Class      string
	Attributes templ.Attributes
}

type MediaProps struct {
	ID         string
	Class      string
	Attributes templ.Attributes
	Variant    MediaVariant
}

type ContentProps struct {
	ID         string
	Class      string
	Attributes templ.Attributes
}

type TitleProps struct {
	ID         string
	Class      string
	Attributes templ.Attributes
}

type DescriptionProps struct {
	ID         string
	Class      string
	Attributes templ.Attributes
}

type ActionsProps struct {
	ID         string
	Class      string
	Attributes templ.Attributes
}

type HeaderProps struct {
	ID         string
	Class      string
	Attributes templ.Attributes
}

type FooterProps struct {
	ID         string
	Class      string
	Attributes templ.Attributes
}

type SeparatorProps struct {
	ID         string
	Class      string
	Attributes templ.Attributes
}

// 1:1 base/ui/item.tsx: structure lives here, the look comes from the
// active style-*.css via the cn-item-* classes.
func itemClasses(variant Variant, size Size) string {
	base := "cn-item group/item flex w-full flex-wrap items-center transition-colors duration-100 outline-none focus-visible:border-ring focus-visible:ring-[3px] focus-visible:ring-ring/50 [a]:transition-colors"
	return utils.TwMerge(base, "cn-item-variant-"+string(variant), "cn-item-size-"+string(size))
}

func mediaClasses(variant MediaVariant) string {
	base := "cn-item-media flex shrink-0 items-center justify-center [&_svg]:pointer-events-none"
	return base + " cn-item-media-variant-" + string(variant)
}

templ Group(props ...GroupProps) {
	{{ var p GroupProps }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	{{ class := "cn-item-group group/item-group flex w-full flex-col" }}
	if p.Class != "" {
		{{ class = class + " " + p.Class }}
	}
	<div
		if p.ID != "" {
			id={ p.ID }
		}
		role="list"
		data-slot="item-group"
		class={ class }
		{ p.Attributes... }
	>
		{ children... }
	</div>
}

templ Item(props ...Props) {
	{{ var p Props }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	if p.Variant == "" {
		{{ p.Variant = VariantDefault }}
	}
	if p.Size == "" {
		{{ p.Size = SizeDefault }}
	}
	if p.Href != "" {
		<a
			if p.ID != "" {
				id={ p.ID }
			}
			href={ templ.SafeURL(p.Href) }
			if p.Target != "" {
				target={ p.Target }
			}
			data-slot="item"
			data-variant={ string(p.Variant) }
			data-size={ string(p.Size) }
			class={ utils.TwMerge(itemClasses(p.Variant, p.Size), p.Class) }
			{ p.Attributes... }
		>
			{ children... }
		</a>
	} else {
		<div
			if p.ID != "" {
				id={ p.ID }
			}
			data-slot="item"
			data-variant={ string(p.Variant) }
			data-size={ string(p.Size) }
			class={ utils.TwMerge(itemClasses(p.Variant, p.Size), p.Class) }
			{ p.Attributes... }
		>
			{ children... }
		</div>
	}
}

templ Media(props ...MediaProps) {
	{{ var p MediaProps }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	if p.Variant == "" {
		{{ p.Variant = MediaVariantDefault }}
	}
	{{ class := mediaClasses(p.Variant) }}
	if p.Class != "" {
		{{ class = class + " " + p.Class }}
	}
	<div
		if p.ID != "" {
			id={ p.ID }
		}
		data-slot="item-media"
		data-variant={ string(p.Variant) }
		class={ class }
		{ p.Attributes... }
	>
		{ children... }
	</div>
}

templ Content(props ...ContentProps) {
	{{ var p ContentProps }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	{{ class := "cn-item-content flex flex-1 flex-col [&+[data-slot=item-content]]:flex-none" }}
	if p.Class != "" {
		{{ class = class + " " + p.Class }}
	}
	<div
		if p.ID != "" {
			id={ p.ID }
		}
		data-slot="item-content"
		class={ class }
		{ p.Attributes... }
	>
		{ children... }
	</div>
}

templ Title(props ...TitleProps) {
	{{ var p TitleProps }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	<div
		if p.ID != "" {
			id={ p.ID }
		}
		data-slot="item-title"
		class={ utils.TwMerge("cn-item-title line-clamp-1 flex w-fit items-center", p.Class) }
		{ p.Attributes... }
	>
		{ children... }
	</div>
}

templ Description(props ...DescriptionProps) {
	{{ var p DescriptionProps }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	{{ class := "cn-item-description line-clamp-2 font-normal [&>a]:underline [&>a]:underline-offset-4 [&>a:hover]:text-primary" }}
	if p.Class != "" {
		{{ class = class + " " + p.Class }}
	}
	<p
		if p.ID != "" {
			id={ p.ID }
		}
		data-slot="item-description"
		class={ class }
		{ p.Attributes... }
	>
		{ children... }
	</p>
}

templ Actions(props ...ActionsProps) {
	{{ var p ActionsProps }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	<div
		if p.ID != "" {
			id={ p.ID }
		}
		data-slot="item-actions"
		class={ utils.TwMerge("cn-item-actions flex items-center", p.Class) }
		{ p.Attributes... }
	>
		{ children... }
	</div>
}

templ Header(props ...HeaderProps) {
	{{ var p HeaderProps }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	<div
		if p.ID != "" {
			id={ p.ID }
		}
		data-slot="item-header"
		class={ utils.TwMerge("cn-item-header flex basis-full items-center justify-between", p.Class) }
		{ p.Attributes... }
	>
		{ children... }
	</div>
}

templ Footer(props ...FooterProps) {
	{{ var p FooterProps }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	<div
		if p.ID != "" {
			id={ p.ID }
		}
		data-slot="item-footer"
		class={ utils.TwMerge("cn-item-footer flex basis-full items-center justify-between", p.Class) }
		{ p.Attributes... }
	>
		{ children... }
	</div>
}

templ Separator(props ...SeparatorProps) {
	{{ var p SeparatorProps }}
	if len(props) > 0 {
		{{ p = props[0] }}
	}
	<div
		if p.ID != "" {
			id={ p.ID }
		}
		data-slot="item-separator"
		data-horizontal
		role="separator"
		aria-orientation="horizontal"
		class={ utils.TwMerge("shrink-0 bg-border data-horizontal:h-px data-horizontal:w-full data-vertical:w-px data-vertical:self-stretch", "cn-item-separator", p.Class) }
		{ p.Attributes... }
	></div>
}
