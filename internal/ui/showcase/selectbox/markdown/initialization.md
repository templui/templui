```javascript
// Initialize all select boxes on the page
window.templUI.selectbox.init();

// Initialize specific select box
window.templUI.selectbox.init("#my-select");

// Initialize with custom selector
window.templUI.selectbox.init(".custom-select");
```

