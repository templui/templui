package charts

import (
	"github.com/templui/templui/components/card"
	"github.com/templui/templui/components/chart"
	"github.com/templui/templui/components/icon"
)

// The pendant of chart-bar-multiple.tsx.

var chartBarMultipleData = []chart.Datum{
	{"month": "January", "desktop": 186, "mobile": 80},
	{"month": "February", "desktop": 305, "mobile": 200},
	{"month": "March", "desktop": 237, "mobile": 120},
	{"month": "April", "desktop": 73, "mobile": 190},
	{"month": "May", "desktop": 209, "mobile": 130},
	{"month": "June", "desktop": 214, "mobile": 140},
}

var chartBarMultipleConfig = chart.Config{
	{Key: "desktop", Label: "Desktop", Color: "var(--chart-1)"},
	{Key: "mobile", Label: "Mobile", Color: "var(--chart-2)"},
}

func barMultipleMonthShort(v any) string {
	return v.(string)[:3]
}

templ ChartBarMultiple() {
	@card.Card() {
		@card.Header() {
			@card.Title() {
				Bar Chart - Multiple
			}
			@card.Description() {
				January - June 2024
			}
		}
		@card.Content() {
			@chart.Container(chart.ContainerProps{ID: "bar-multiple", Config: chartBarMultipleConfig}) {
				@chart.BarChart(chart.BarChartProps{Data: chartBarMultipleData}) {
					@chart.CartesianGrid(chart.CartesianGridProps{Vertical: chart.Bool(false)})
					@chart.XAxis(chart.XAxisProps{
						DataKey:       "month",
						TickMargin:    10,
						TickFormatter: barMultipleMonthShort,
					})
					@chart.Tooltip(chart.TooltipProps{
						Cursor:  chart.Bool(false),
						Content: chart.TooltipContentProps{Indicator: "dashed"},
					})
					@chart.Bar(chart.BarProps{DataKey: "desktop", Fill: "var(--color-desktop)", Radius: 4})
					@chart.Bar(chart.BarProps{DataKey: "mobile", Fill: "var(--color-mobile)", Radius: 4})
				}
			}
		}
		@card.Footer(card.FooterProps{Class: "flex-col items-start gap-2 text-sm"}) {
			<div class="flex gap-2 leading-none font-medium">
				Trending up by 5.2% this month
				@icon.TrendingUp(icon.Props{Class: "h-4 w-4"})
			</div>
			<div class="leading-none text-muted-foreground">
				Showing total visitors for the last 6 months
			</div>
		}
	}
}
