package charts

import (
	"github.com/templui/templui/components/card"
	"github.com/templui/templui/components/chart"
	"github.com/templui/templui/components/icon"
)

// The pendant of chart-bar-horizontal.tsx.

var chartBarHorizontalData = []chart.Datum{
	{"month": "January", "desktop": 186},
	{"month": "February", "desktop": 305},
	{"month": "March", "desktop": 237},
	{"month": "April", "desktop": 73},
	{"month": "May", "desktop": 209},
	{"month": "June", "desktop": 214},
}

var chartBarHorizontalConfig = chart.Config{
	{Key: "desktop", Label: "Desktop", Color: "var(--chart-1)"},
}

func barHorizontalMonthShort(v any) string {
	return v.(string)[:3]
}

templ ChartBarHorizontal() {
	@card.Card() {
		@card.Header() {
			@card.Title() {
				Bar Chart - Horizontal
			}
			@card.Description() {
				January - June 2024
			}
		}
		@card.Content() {
			@chart.Container(chart.ContainerProps{ID: "bar-horizontal", Config: chartBarHorizontalConfig}) {
				@chart.BarChart(chart.BarChartProps{
					AccessibilityLayer: true,
					Data:   chartBarHorizontalData,
					Layout: "vertical",
					Margin: &chart.Margin{Left: -20},
				}) {
					@chart.XAxis(chart.XAxisProps{Type: "number", DataKey: "desktop", Hide: true})
					@chart.YAxis(chart.YAxisProps{
						DataKey:       "month",
						Type:          "category",
						TickMargin:    10,
						TickFormatter: barHorizontalMonthShort,
					})
					@chart.Tooltip(chart.TooltipProps{
						Cursor:  chart.Bool(false),
						Content: chart.TooltipContentProps{HideLabel: true},
					})
					@chart.Bar(chart.BarProps{DataKey: "desktop", Fill: "var(--color-desktop)", Radius: 5})
				}
			}
		}
		@card.Footer(card.FooterProps{Class: "flex-col items-start gap-2 text-sm"}) {
			<div class="flex gap-2 leading-none font-medium">
				Trending up by 5.2% this month
				@icon.TrendingUp(icon.Props{Class: "h-4 w-4"})
			</div>
			<div class="leading-none text-muted-foreground">
				Showing total visitors for the last 6 months
			</div>
		}
	}
}
