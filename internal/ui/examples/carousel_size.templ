package examples

import (
	"strconv"

	"github.com/templui/templui/components/card"
	"github.com/templui/templui/components/carousel"
)

templ CarouselSize() {
	@carousel.Carousel(carousel.Props{
		Align: carousel.AlignStart,Class: "w-full max-w-[12rem] sm:max-w-xs md:max-w-sm"}) {
		@carousel.Content() {
			for i := 1; i <= 5; i++ {
				@carousel.Item(carousel.ItemProps{Class: "basis-1/2 lg:basis-1/3"}) {
					<div class="p-1">
						@card.Card() {
							@card.Content(card.ContentProps{Class: "flex aspect-square items-center justify-center p-6"}) {
								<span class="text-3xl font-semibold">{ strconv.Itoa(i) }</span>
							}
						}
					</div>
				}
			}
		}
		@carousel.Previous()
		@carousel.Next()
	}
}
