package examples

import (
	"fmt"

	"github.com/templui/templui/components/button"
	"github.com/templui/templui/components/card"
	"github.com/templui/templui/components/combobox"
	"github.com/templui/templui/components/dialog"
	"github.com/templui/templui/components/field"
	"github.com/templui/templui/components/icon"
	"github.com/templui/templui/components/input"
	"github.com/templui/templui/components/inputgroup"
	"github.com/templui/templui/components/item"
	selectcomp "github.com/templui/templui/components/select"
	"github.com/templui/templui/utils"
)

// The kitchen-sink page from combobox-example.tsx.
templ comboboxExampleSection(title string, containerClass string) {
	<div data-slot="example" class={ utils.CN("mx-auto flex w-full max-w-lg min-w-0 flex-col gap-1 self-stretch lg:max-w-none", containerClass) }>
		if title != "" {
			<div class="px-1.5 py-2 text-xs font-medium text-muted-foreground">{ title }</div>
		}
		<div data-slot="example-content" class="flex min-w-0 flex-1 flex-col items-start gap-6 rounded-xl bg-card p-12 text-foreground style-lyra:rounded-none style-sera:rounded-none *:[div:not([class*='w-'])]:w-full">
			{ children... }
		</div>
	</div>
}

templ ComboboxExample() {
	<div class="w-full bg-muted dark:bg-background">
		<div data-slot="example-wrapper" class="mx-auto grid min-h-screen w-full max-w-5xl min-w-0 content-center items-start gap-8 p-4 pt-2 sm:gap-12 sm:p-6 md:grid-cols-2 md:gap-8 lg:p-12 2xl:max-w-6xl">
			@comboboxExampleBasic()
			@comboboxExampleDisabled()
			@comboboxExampleSides()
			@comboboxExampleInvalid()
			@comboboxExampleWithClear()
			@comboboxExampleAutoHighlight()
			@comboboxExampleWithGroups()
			@comboboxExampleWithGroupsAndSeparator()
			@comboboxExampleLargeList()
			@comboboxExampleInputAddon()
			@comboboxExampleInPopup()
			@comboboxExampleWithForm()
			@comboboxExampleMultiple()
			@comboboxExampleMultipleDisabled()
			@comboboxExampleMultipleInvalid()
			@comboboxExampleMultipleNoRemove()
			@comboboxExampleWithCustomItems()
			@comboboxExampleInDialog()
			@comboboxExampleWithOtherInputs()
			@comboboxExampleDisabledItems()
		</div>
	</div>
}

type comboboxExampleFramework struct {
	Value string
	Label string
}

var comboboxExampleFrameworks = []comboboxExampleFramework{
	{"templ", "templ"},
	{"htmx", "htmx"},
	{"gin", "Gin"},
	{"echo", "Echo"},
	{"chi", "Chi"},
}

type comboboxExampleTimezoneGroup struct {
	Value string
	Items []comboboxExampleFramework
}

var comboboxExampleTimezones = []comboboxExampleTimezoneGroup{
	{"Americas", []comboboxExampleFramework{
		{"new-york", "(GMT-5) New York"},
		{"los-angeles", "(GMT-8) Los Angeles"},
		{"chicago", "(GMT-6) Chicago"},
		{"toronto", "(GMT-5) Toronto"},
		{"vancouver", "(GMT-8) Vancouver"},
		{"sao-paulo", "(GMT-3) São Paulo"},
	}},
	{"Europe", []comboboxExampleFramework{
		{"london", "(GMT+0) London"},
		{"paris", "(GMT+1) Paris"},
		{"berlin", "(GMT+1) Berlin"},
		{"rome", "(GMT+1) Rome"},
		{"madrid", "(GMT+1) Madrid"},
		{"amsterdam", "(GMT+1) Amsterdam"},
	}},
	{"Asia/Pacific", []comboboxExampleFramework{
		{"tokyo", "(GMT+9) Tokyo"},
		{"shanghai", "(GMT+8) Shanghai"},
		{"singapore", "(GMT+8) Singapore"},
		{"dubai", "(GMT+4) Dubai"},
		{"sydney", "(GMT+11) Sydney"},
		{"seoul", "(GMT+9) Seoul"},
	}},
}

templ comboboxExampleFrameworkItems() {
	for _, fw := range comboboxExampleFrameworks {
		@combobox.Item(combobox.ItemProps{Value: fw.Value, Label: fw.Label}) {
			{ fw.Label }
		}
	}
}

templ comboboxExampleBasic() {
	@comboboxExampleSection("Basic", "") {
		@combobox.Combobox() {
			@combobox.Input(combobox.InputProps{Placeholder: "Select a framework"})
			@combobox.Content() {
				@combobox.Empty() {
					No items found.
				}
				@combobox.List() {
					@comboboxExampleFrameworkItems()
				}
			}
		}
	}
}

templ comboboxExampleDisabled() {
	@comboboxExampleSection("Disabled", "") {
		@combobox.Combobox(combobox.Props{Disabled: true}) {
			@combobox.Input(combobox.InputProps{Placeholder: "Select a framework"})
			@combobox.Content() {
				@combobox.Empty() {
					No items found.
				}
				@combobox.List() {
					@comboboxExampleFrameworkItems()
				}
			}
		}
	}
}

templ comboboxExampleSides() {
	@comboboxExampleSection("Sides", "col-span-2") {
		<div class="flex flex-wrap justify-center gap-2">
			@comboboxExampleSide("inline start", combobox.SideLeft)
			@comboboxExampleSide("left", combobox.SideLeft)
			@comboboxExampleSide("top", combobox.SideTop)
			@comboboxExampleSide("bottom", combobox.SideBottom)
			@comboboxExampleSide("right", combobox.SideRight)
			@comboboxExampleSide("inline end", combobox.SideRight)
		</div>
	}
}

templ comboboxExampleSide(placeholder string, side combobox.Side) {
	@combobox.Combobox() {
		@combobox.Input(combobox.InputProps{
			Placeholder: placeholder,
			Class:       "w-32 **:data-[slot=input-group-control]:capitalize",
		})
		@combobox.Content(combobox.ContentProps{Side: side}) {
			@combobox.Empty() {
				No items found.
			}
			@combobox.List() {
				@comboboxExampleFrameworkItems()
			}
		}
	}
}

templ comboboxExampleInvalid() {
	@comboboxExampleSection("Invalid", "") {
		<div class="flex flex-col gap-4">
			@combobox.Combobox() {
				@combobox.Input(combobox.InputProps{
					Placeholder: "Select a framework",
					Attributes:  templ.Attributes{"aria-invalid": "true"},
				})
				@combobox.Content() {
					@combobox.Empty() {
						No items found.
					}
					@combobox.List() {
						@comboboxExampleFrameworkItems()
					}
				}
			}
			@field.Field(field.Props{Invalid: true}) {
				@field.Label(field.LabelProps{For: "combobox-example-framework-invalid"}) {
					Framework
				}
				@combobox.Combobox() {
					@combobox.Input(combobox.InputProps{
						ID:          "combobox-example-framework-invalid",
						Placeholder: "Select a framework",
						Attributes:  templ.Attributes{"aria-invalid": "true"},
					})
					@combobox.Content() {
						@combobox.Empty() {
							No items found.
						}
						@combobox.List() {
							@comboboxExampleFrameworkItems()
						}
					}
				}
				@field.Description() {
					Please select a valid framework.
				}
				@field.Error() {
					This field is required.
				}
			}
		</div>
	}
}

templ comboboxExampleWithClear() {
	@comboboxExampleSection("With Clear Button", "") {
		@combobox.Combobox(combobox.Props{Value: "templ"}) {
			@combobox.Input(combobox.InputProps{Placeholder: "Select a framework", ShowClear: true})
			@combobox.Content() {
				@combobox.Empty() {
					No items found.
				}
				@combobox.List() {
					@comboboxExampleFrameworkItems()
				}
			}
		}
	}
}

templ comboboxExampleAutoHighlight() {
	@comboboxExampleSection("With Auto Highlight", "") {
		@combobox.Combobox(combobox.Props{AutoHighlight: true}) {
			@combobox.Input(combobox.InputProps{Placeholder: "Select a framework"})
			@combobox.Content() {
				@combobox.Empty() {
					No items found.
				}
				@combobox.List() {
					@comboboxExampleFrameworkItems()
				}
			}
		}
	}
}

templ comboboxExampleWithGroups() {
	@comboboxExampleSection("With Groups", "") {
		@combobox.Combobox() {
			@combobox.Input(combobox.InputProps{Placeholder: "Select a timezone"})
			@combobox.Content() {
				@combobox.Empty() {
					No timezones found.
				}
				@combobox.List() {
					for _, group := range comboboxExampleTimezones {
						@combobox.Group() {
							@combobox.Label() {
								{ group.Value }
							}
							for _, tz := range group.Items {
								@combobox.Item(combobox.ItemProps{Value: tz.Value, Label: tz.Label}) {
									{ tz.Label }
								}
							}
						}
					}
				}
			}
		}
	}
}

templ comboboxExampleWithGroupsAndSeparator() {
	@comboboxExampleSection("With Groups and Separator", "") {
		@combobox.Combobox() {
			@combobox.Input(combobox.InputProps{Placeholder: "Select a timezone"})
			@combobox.Content() {
				@combobox.Empty() {
					No timezones found.
				}
				@combobox.List() {
					for _, group := range comboboxExampleTimezones {
						@combobox.Group() {
							@combobox.Label() {
								{ group.Value }
							}
							for _, tz := range group.Items {
								@combobox.Item(combobox.ItemProps{Value: tz.Value, Label: tz.Label}) {
									{ tz.Label }
								}
							}
							@combobox.Separator()
						}
					}
				}
			}
		}
	}
}

templ comboboxExampleLargeList() {
	@comboboxExampleSection("Large List (100 items)", "") {
		@combobox.Combobox() {
			@combobox.Input(combobox.InputProps{Placeholder: "Search from 100 items"})
			@combobox.Content() {
				@combobox.Empty() {
					No items found.
				}
				@combobox.List() {
					for i := 1; i <= 100; i++ {
						@combobox.Item(combobox.ItemProps{Value: fmt.Sprintf("Item %d", i)}) {
							{ fmt.Sprintf("Item %d", i) }
						}
					}
				}
			}
		}
	}
}

templ comboboxExampleInputAddon() {
	@comboboxExampleSection("With Icon Addon", "") {
		@combobox.Combobox() {
			@combobox.Input(combobox.InputProps{Placeholder: "Select a timezone"}) {
				@inputgroup.Addon() {
					@icon.Globe()
				}
			}
			@combobox.Content(combobox.ContentProps{
				AlignOffset: -28,
				Class:       "w-60",
			}) {
				@combobox.Empty() {
					No timezones found.
				}
				@combobox.List() {
					for _, group := range comboboxExampleTimezones {
						@combobox.Group() {
							@combobox.Label() {
								{ group.Value }
							}
							for _, tz := range group.Items {
								@combobox.Item(combobox.ItemProps{Value: tz.Value, Label: tz.Label}) {
									{ tz.Label }
								}
							}
						}
					}
				}
			}
		}
	}
}

templ comboboxExampleInPopup() {
	@comboboxExampleSection("Combobox in Popup", "") {
		@combobox.Combobox() {
			@button.Button(button.Props{
				Variant:    button.VariantOutline,
				Class:      "w-64 justify-between font-normal",
				Attributes: combobox.Trigger(ctx),
			}) {
				@combobox.Value(combobox.ValueProps{Placeholder: "Select country"})
				@icon.ChevronDown(icon.Props{Class: "pointer-events-none size-4 text-muted-foreground"})
			}
			@combobox.Content() {
				@combobox.Input(combobox.InputProps{
					Placeholder: "Search",
					HideTrigger: true,
				})
				@combobox.Empty() {
					No items found.
				}
				@combobox.List() {
					for _, country := range comboboxCountries {
						@combobox.Item(combobox.ItemProps{Value: country.Value, Label: country.Label}) {
							{ country.Label }
						}
					}
				}
			}
		}
	}
}

templ comboboxExampleWithForm() {
	@comboboxExampleSection("Form with Combobox", "") {
		@card.Card(card.Props{Class: "w-full max-w-sm", Size: card.SizeSm}) {
			@card.Content() {
				<form id="combobox-example-form" class="w-full">
					@field.Group() {
						@field.Field() {
							@field.Label(field.LabelProps{For: "combobox-example-framework"}) {
								Framework
							}
							@combobox.Combobox(combobox.Props{Name: "framework"}) {
								@combobox.Input(combobox.InputProps{
									ID:          "combobox-example-framework",
									Placeholder: "Select a framework",
									Attributes:  templ.Attributes{"required": true},
								})
								@combobox.Content() {
									@combobox.Empty() {
										No items found.
									}
									@combobox.List() {
										@comboboxExampleFrameworkItems()
									}
								}
							}
						}
					}
				</form>
			}
			@card.Footer() {
				@button.Button(button.Props{Type: button.TypeSubmit, Form: "combobox-example-form"}) {
					Submit
				}
			}
		}
	}
	<script nonce={ templ.GetNonce(ctx) }>
		// The vanilla pendant of the demo's onSubmit toast.
		document.addEventListener("submit", (e) => {
			if (e.target.id !== "combobox-example-form") return;
			e.preventDefault();
			const framework = new FormData(e.target).get("framework");
			window.tui.toast.add({ title: `You selected ${framework} as your framework.` });
		});
	</script>
}

templ comboboxExampleMultiple() {
	@comboboxExampleSection("Combobox Multiple", "") {
		@combobox.Combobox(combobox.Props{
			Multiple:      true,
			AutoHighlight: true,
			Values:        []string{"templ"},
		}) {
			@combobox.Chips() {
				@combobox.Value()
				@combobox.ChipsInput()
			}
			@combobox.Content() {
				@combobox.Empty() {
					No items found.
				}
				@combobox.List() {
					@comboboxExampleFrameworkItems()
				}
			}
		}
	}
}

templ comboboxExampleMultipleDisabled() {
	@comboboxExampleSection("Combobox Multiple Disabled", "") {
		@combobox.Combobox(combobox.Props{
			Multiple:      true,
			AutoHighlight: true,
			Values:        []string{"templ", "htmx"},
			Disabled:      true,
		}) {
			@combobox.Chips() {
				@combobox.Value()
				@combobox.ChipsInput(combobox.ChipsInputProps{Attributes: templ.Attributes{"disabled": true}})
			}
			@combobox.Content() {
				@combobox.Empty() {
					No items found.
				}
				@combobox.List() {
					@comboboxExampleFrameworkItems()
				}
			}
		}
	}
}

templ comboboxExampleMultipleInvalid() {
	@comboboxExampleSection("Combobox Multiple Invalid", "") {
		<div class="flex flex-col gap-4">
			@combobox.Combobox(combobox.Props{
				Multiple:      true,
				AutoHighlight: true,
				Values:        []string{"templ", "htmx"},
			}) {
				@combobox.Chips() {
					@combobox.Value()
					@combobox.ChipsInput(combobox.ChipsInputProps{Attributes: templ.Attributes{"aria-invalid": "true"}})
				}
				@combobox.Content() {
					@combobox.Empty() {
						No items found.
					}
					@combobox.List() {
						@comboboxExampleFrameworkItems()
					}
				}
			}
			@field.Field(field.Props{Invalid: true}) {
				@field.Label(field.LabelProps{For: "combobox-example-multiple-invalid"}) {
					Frameworks
				}
				@combobox.Combobox(combobox.Props{
					Multiple:      true,
					AutoHighlight: true,
					Values:        []string{"templ", "htmx", "gin"},
				}) {
					@combobox.Chips() {
						@combobox.Value()
						@combobox.ChipsInput(combobox.ChipsInputProps{
							ID:         "combobox-example-multiple-invalid",
							Attributes: templ.Attributes{"aria-invalid": "true"},
						})
					}
					@combobox.Content() {
						@combobox.Empty() {
							No items found.
						}
						@combobox.List() {
							@comboboxExampleFrameworkItems()
						}
					}
				}
				@field.Description() {
					Please select at least one framework.
				}
				@field.Error() {
					This field is required.
				}
			}
		</div>
	}
}

templ comboboxExampleMultipleNoRemove() {
	@comboboxExampleSection("Combobox Multiple (No Remove)", "") {
		@combobox.Combobox(combobox.Props{
			Multiple:      true,
			AutoHighlight: true,
			Values:        []string{"templ", "htmx"},
		}) {
			@combobox.Chips(combobox.ChipsProps{Class: "[&_[data-tui-combobox-chip-remove]]:hidden"}) {
				@combobox.Value()
				@combobox.ChipsInput()
			}
			@combobox.Content() {
				@combobox.Empty() {
					No items found.
				}
				@combobox.List() {
					@comboboxExampleFrameworkItems()
				}
			}
		}
	}
}

templ comboboxExampleWithCustomItems() {
	@comboboxExampleSection("With Custom Item Rendering", "") {
		@combobox.Combobox() {
			@combobox.Input(combobox.InputProps{Placeholder: "Search countries..."})
			@combobox.Content() {
				@combobox.Empty() {
					No countries found.
				}
				@combobox.List() {
					for _, country := range comboboxCountries {
						@combobox.Item(combobox.ItemProps{Value: country.Value, Label: country.Label}) {
							@item.Item(item.Props{Size: item.SizeXs, Class: "p-0"}) {
								@item.Content() {
									@item.Title(item.TitleProps{Class: "whitespace-nowrap"}) {
										{ country.Label }
									}
									@item.Description() {
										{ country.Continent } ({ country.Code })
									}
								}
							}
						}
					}
				}
			}
		}
	}
}

templ comboboxExampleInDialog() {
	@comboboxExampleSection("Combobox in Dialog", "") {
		@dialog.Dialog() {
			@button.Button(button.Props{
				Variant:    button.VariantOutline,
				Attributes: dialog.Trigger(ctx),
			}) {
				Open Dialog
			}
			@dialog.Content(dialog.ContentProps{Class: "sm:max-w-[425px]"}) {
				@dialog.Header() {
					@dialog.Title() {
						Select Framework
					}
					@dialog.Description() {
						Choose your preferred framework from the list below.
					}
				}
				@field.Field() {
					@field.Label(field.LabelProps{For: "combobox-example-framework-dialog", Class: "sr-only"}) {
						Framework
					}
					@combobox.Combobox() {
						@combobox.Input(combobox.InputProps{
							ID:          "combobox-example-framework-dialog",
							Placeholder: "Select a framework",
						})
						@combobox.Content() {
							@combobox.Empty() {
								No items found.
							}
							@combobox.List() {
								@comboboxExampleFrameworkItems()
							}
						}
					}
				}
				@dialog.Footer() {
					@button.Button(button.Props{
						Type:       button.TypeButton,
						Variant:    button.VariantOutline,
						Attributes: dialog.Close(ctx),
					}) {
						Cancel
					}
					@button.Button(button.Props{
						Type:       button.TypeButton,
						Attributes: utils.MergeAttributes(dialog.Close(ctx), templ.Attributes{"id": "combobox-example-dialog-confirm"}),
					}) {
						Confirm
					}
				}
			}
		}
	}
	<script nonce={ templ.GetNonce(ctx) }>
		// The vanilla pendant of the demo's confirm toast.
		document.addEventListener("click", (e) => {
			if (!(e.target instanceof Element) || !e.target.closest("#combobox-example-dialog-confirm")) return;
			window.tui.toast.add({ title: "Framework selected." });
		});
	</script>
}

templ comboboxExampleWithOtherInputs() {
	@comboboxExampleSection("With Other Inputs", "") {
		@combobox.Combobox() {
			@combobox.Input(combobox.InputProps{Placeholder: "Select a framework", Class: "w-52"})
			@combobox.Content() {
				@combobox.Empty() {
					No items found.
				}
				@combobox.List() {
					@comboboxExampleFrameworkItems()
				}
			}
		}
		@selectcomp.Select() {
			@selectcomp.Trigger(selectcomp.TriggerProps{Class: "w-52"}) {
				@selectcomp.Value(selectcomp.ValueProps{Placeholder: "Select a framework"})
			}
			@selectcomp.Content() {
				@selectcomp.Group() {
					for _, fw := range comboboxExampleFrameworks {
						@selectcomp.Item(selectcomp.ItemProps{Value: fw.Value, Label: fw.Label}) {
							{ fw.Label }
						}
					}
				}
			}
		}
		@button.Button(button.Props{
			Variant: button.VariantOutline,
			Class:   "w-52 justify-between font-normal text-muted-foreground",
		}) {
			Select a framework
			@icon.ChevronDown()
		}
		@input.Input(input.Props{Placeholder: "Select a framework", Class: "w-52"})
		@inputgroup.InputGroup(inputgroup.Props{Class: "w-52"}) {
			@inputgroup.Input(inputgroup.InputProps{Placeholder: "Select a framework"})
			@inputgroup.Addon(inputgroup.AddonProps{Align: inputgroup.AlignInlineEnd}) {
				@icon.ChevronDown()
			}
		}
	}
}

templ comboboxExampleDisabledItems() {
	@comboboxExampleSection("Disabled Items", "") {
		@combobox.Combobox() {
			@combobox.Input(combobox.InputProps{Placeholder: "Select a framework"})
			@combobox.Content() {
				@combobox.Empty() {
					No items found.
				}
				@combobox.List() {
					for _, fw := range comboboxExampleFrameworks {
						@combobox.Item(combobox.ItemProps{
							Value:    fw.Value,
							Label:    fw.Label,
							Disabled: fw.Value == "gin" || fw.Value == "echo",
						}) {
							{ fw.Label }
						}
					}
				}
			}
		}
	}
}
