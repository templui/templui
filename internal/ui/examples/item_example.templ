package examples

import (
	"github.com/templui/templui/components/button"
	"github.com/templui/templui/components/icon"
	"github.com/templui/templui/components/item"
)

// The kitchen-sink page from item-example.tsx. The nine variant/size
// sections and the image sections repeat the same item combinations, so
// they render through the shared matrix helpers below.
templ itemExampleSection(title string) {
	<div data-slot="example" class="mx-auto flex w-full max-w-lg min-w-0 flex-col gap-1 self-stretch lg:max-w-none">
		if title != "" {
			<div class="px-1.5 py-2 text-xs font-medium text-muted-foreground">{ title }</div>
		}
		<div data-slot="example-content" class="flex min-w-0 flex-1 flex-col items-start gap-6 rounded-xl bg-card p-12 text-foreground style-lyra:rounded-none style-sera:rounded-none *:[div:not([class*='w-'])]:w-full">
			{ children... }
		</div>
	</div>
}

templ ItemExample() {
	<div class="w-full bg-muted dark:bg-background">
		<div data-slot="example-wrapper" class="mx-auto grid min-h-screen w-full max-w-5xl min-w-0 content-center items-start gap-8 p-4 pt-2 sm:gap-12 sm:p-6 md:grid-cols-2 md:gap-8 lg:p-12 2xl:max-w-6xl">
			@itemExampleVariantSection("Default", item.VariantDefault, item.SizeDefault, "")
			@itemExampleVariantSection("Outline", item.VariantOutline, item.SizeDefault, "")
			@itemExampleVariantSection("Muted", item.VariantMuted, item.SizeDefault, "")
			@itemExampleVariantSection("Small", item.VariantDefault, item.SizeSm, button.SizeSm)
			@itemExampleVariantSection("Outline - Small", item.VariantOutline, item.SizeSm, button.SizeSm)
			@itemExampleVariantSection("Muted - Small", item.VariantMuted, item.SizeSm, button.SizeSm)
			@itemExampleVariantSection("Extra Small", item.VariantDefault, item.SizeXs, button.SizeSm)
			@itemExampleVariantSection("Outline - Extra Small", item.VariantOutline, item.SizeXs, button.SizeSm)
			@itemExampleVariantSection("Muted - Extra Small", item.VariantMuted, item.SizeXs, button.SizeSm)
			@itemExampleLinkSection("asChild", item.VariantDefault)
			@itemExampleLinkSection("Outline - asChild", item.VariantOutline)
			@itemExampleLinkSection("Muted - asChild", item.VariantMuted)
			@itemExampleDefaultGroup()
			@itemExampleOutlineGroup()
			@itemExampleMutedGroup()
			@itemExampleSeparatorSection()
			@itemExampleHeaderSection()
			@itemExampleFooterSection()
			@itemExampleHeaderAndFooterSection()
			@itemExampleImageSection("Default - ItemMedia image", item.VariantDefault, item.SizeDefault, true)
			@itemExampleImageSection("Outline - ItemMedia image", item.VariantOutline, item.SizeDefault, true)
			@itemExampleImageSection("Outline - ItemMedia image - Small", item.VariantOutline, item.SizeSm, true)
			@itemExampleImageSection("Outline - ItemMedia image - Extra Small", item.VariantOutline, item.SizeXs, false)
			@itemExampleImageSection("Muted - ItemMedia image", item.VariantMuted, item.SizeDefault, true)
		</div>
	</div>
}

// itemExampleVariantSection is one of the nine variant/size sections: the
// same item combinations, only variant, size and the outline action button
// size differ (the default sections use the default button size).
templ itemExampleVariantSection(title string, variant item.Variant, size item.Size, outlineButtonSize button.Size) {
	@itemExampleSection(title) {
		@item.Item(item.Props{Variant: variant, Size: size}) {
			@item.Content() {
				@item.Title() {
					Title Only
				}
			}
		}
		@item.Item(item.Props{Variant: variant, Size: size}) {
			@item.Content() {
				@item.Title() {
					Title + Button
				}
			}
			@item.Actions() {
				@button.Button(button.Props{Variant: button.VariantOutline, Size: outlineButtonSize}) {
					Action
				}
			}
		}
		@item.Item(item.Props{Variant: variant, Size: size}) {
			@item.Content() {
				@item.Title() {
					Title + Description
				}
				@item.Description() {
					This is a description that provides additional context.
				}
			}
		}
		@item.Item(item.Props{Variant: variant, Size: size}) {
			@item.Content() {
				@item.Title() {
					Title + Description + Button
				}
				@item.Description() {
					This item includes a title, description, and action button.
				}
			}
			@item.Actions() {
				@button.Button(button.Props{Variant: button.VariantOutline, Size: outlineButtonSize}) {
					Action
				}
			}
		}
		@item.Item(item.Props{Variant: variant, Size: size}) {
			@item.Media(item.MediaProps{Variant: item.MediaVariantIcon}) {
				@icon.Inbox()
			}
			@item.Content() {
				@item.Title() {
					Media + Title
				}
			}
		}
		@item.Item(item.Props{Variant: variant, Size: size}) {
			@item.Media(item.MediaProps{Variant: item.MediaVariantIcon}) {
				@icon.Inbox()
			}
			@item.Content() {
				@item.Title() {
					Media + Title + Button
				}
			}
			@item.Actions() {
				@button.Button(button.Props{Size: button.SizeSm}) {
					Action
				}
			}
		}
		@item.Item(item.Props{Variant: variant, Size: size}) {
			@item.Media(item.MediaProps{Variant: item.MediaVariantIcon}) {
				@icon.Inbox()
			}
			@item.Content() {
				@item.Title() {
					Media + Title + Description
				}
				@item.Description() {
					This item includes media, title, and description.
				}
			}
		}
		@item.Item(item.Props{Variant: variant, Size: size}) {
			@item.Media(item.MediaProps{Variant: item.MediaVariantIcon}) {
				@icon.Inbox()
			}
			@item.Content() {
				@item.Title() {
					Media + Title + Description + Button
				}
				@item.Description() {
					Complete item with all components: media, title, description, and button.
				}
			}
			@item.Actions() {
				@button.Button(button.Props{Size: button.SizeSm}) {
					Action
				}
			}
		}
		@item.Item(item.Props{Variant: variant, Size: size}) {
			@item.Content() {
				@item.Title() {
					Multiple Actions
				}
				@item.Description() {
					Item with multiple action buttons in the actions area.
				}
			}
			@item.Actions() {
				@button.Button(button.Props{Variant: button.VariantOutline, Size: button.SizeSm}) {
					Cancel
				}
				@button.Button(button.Props{Size: button.SizeSm}) {
					Confirm
				}
			}
		}
	}
}

templ itemExampleLinkSection(title string, variant item.Variant) {
	@itemExampleSection(title) {
		@item.Group() {
			@item.Item(item.Props{Variant: variant, Href: "#"}) {
				@item.Content() {
					@item.Title() {
						Title Only (Link)
					}
				}
			}
			@item.Item(item.Props{Variant: variant, Href: "#"}) {
				@item.Content() {
					@item.Title() {
						Title + Description (Link)
					}
					@item.Description() {
						Clickable item with title and description.
					}
				}
			}
			@item.Item(item.Props{Variant: variant, Href: "#"}) {
				@item.Media(item.MediaProps{Variant: item.MediaVariantIcon}) {
					@icon.Inbox()
				}
				@item.Content() {
					@item.Title() {
						Media + Title (Link)
					}
				}
			}
			@item.Item(item.Props{Variant: variant, Href: "#"}) {
				@item.Media(item.MediaProps{Variant: item.MediaVariantIcon}) {
					@icon.Inbox()
				}
				@item.Content() {
					@item.Title() {
						Media + Title + Description (Link)
					}
					@item.Description() {
						Complete link item with media, title, and description.
					}
				}
			}
			@item.Item(item.Props{Variant: variant, Href: "#"}) {
				@item.Content() {
					@item.Title() {
						With Actions (Link)
					}
					@item.Description() {
						Link item that also has action buttons.
					}
				}
				@item.Actions() {
					@button.Button(button.Props{Variant: button.VariantOutline, Size: button.SizeSm}) {
						Share
					}
				}
			}
		}
	}
}

templ itemExampleDefaultGroup() {
	@itemExampleSection("ItemGroup") {
		@item.Group() {
			@item.Item() {
				@item.Content() {
					@item.Title() {
						Item 1
					}
					@item.Description() {
						First item in the group.
					}
				}
			}
			@item.Item() {
				@item.Content() {
					@item.Title() {
						Item 2
					}
					@item.Description() {
						Second item in the group.
					}
				}
			}
			@item.Item() {
				@item.Content() {
					@item.Title() {
						Item 3
					}
					@item.Description() {
						Third item in the group.
					}
				}
			}
		}
	}
}

templ itemExampleOutlineGroup() {
	@itemExampleSection("Outline - ItemGroup") {
		@item.Group() {
			@item.Item(item.Props{Variant: item.VariantOutline}) {
				@item.Media(item.MediaProps{Variant: item.MediaVariantIcon}) {
					@icon.Inbox()
				}
				@item.Content() {
					@item.Title() {
						Item 1
					}
					@item.Description() {
						First item with icon.
					}
				}
			}
			@item.Item(item.Props{Variant: item.VariantOutline}) {
				@item.Media(item.MediaProps{Variant: item.MediaVariantIcon}) {
					@icon.Inbox()
				}
				@item.Content() {
					@item.Title() {
						Item 2
					}
					@item.Description() {
						Second item with icon.
					}
				}
			}
			@item.Item(item.Props{Variant: item.VariantOutline}) {
				@item.Media(item.MediaProps{Variant: item.MediaVariantIcon}) {
					@icon.Inbox()
				}
				@item.Content() {
					@item.Title() {
						Item 3
					}
					@item.Description() {
						Third item with icon.
					}
				}
			}
		}
	}
}

templ itemExampleMutedGroup() {
	@itemExampleSection("Muted - ItemGroup") {
		@item.Group() {
			@item.Item(item.Props{Variant: item.VariantMuted}) {
				@item.Content() {
					@item.Title() {
						Item 1
					}
					@item.Description() {
						First item in muted group.
					}
				}
				@item.Actions() {
					@button.Button(button.Props{Variant: button.VariantOutline, Size: button.SizeSm}) {
						Action
					}
				}
			}
			@item.Item(item.Props{Variant: item.VariantMuted}) {
				@item.Content() {
					@item.Title() {
						Item 2
					}
					@item.Description() {
						Second item in muted group.
					}
				}
				@item.Actions() {
					@button.Button(button.Props{Variant: button.VariantOutline, Size: button.SizeSm}) {
						Action
					}
				}
			}
			@item.Item(item.Props{Variant: item.VariantMuted}) {
				@item.Content() {
					@item.Title() {
						Item 3
					}
					@item.Description() {
						Third item in muted group.
					}
				}
				@item.Actions() {
					@button.Button(button.Props{Variant: button.VariantOutline, Size: button.SizeSm}) {
						Action
					}
				}
			}
		}
	}
}

templ itemExampleSeparatorItem(title, description string) {
	@item.Item(item.Props{Variant: item.VariantOutline}) {
		@item.Media(item.MediaProps{Variant: item.MediaVariantIcon}) {
			@icon.Inbox()
		}
		@item.Content() {
			@item.Title() {
				{ title }
			}
			@item.Description() {
				{ description }
			}
		}
	}
}

templ itemExampleSeparatorSection() {
	@itemExampleSection("ItemSeparator") {
		@item.Group() {
			@itemExampleSeparatorItem("Inbox", "View all incoming messages.")
			@item.Separator()
			@itemExampleSeparatorItem("Sent", "View all sent messages.")
			@item.Separator()
			@itemExampleSeparatorItem("Drafts", "View all draft messages.")
			@item.Separator()
			@itemExampleSeparatorItem("Archive", "View archived messages.")
		}
	}
}

templ itemExampleHeaderSection() {
	@itemExampleSection("ItemHeader") {
		@item.Item() {
			@item.Header() {
				<span class="text-sm font-medium">Design System</span>
			}
			@item.Content() {
				@item.Title() {
					Component Library
				}
				@item.Description() {
					A comprehensive collection of reusable UI components for building consistent interfaces.
				}
			}
		}
		@item.Item(item.Props{Variant: item.VariantOutline}) {
			@item.Header() {
				<span class="text-sm font-medium">Marketing</span>
			}
			@item.Content() {
				@item.Title() {
					Campaign Analytics
				}
				@item.Description() {
					Track performance metrics and engagement rates across all marketing channels.
				}
			}
		}
		@item.Item(item.Props{Variant: item.VariantMuted}) {
			@item.Header() {
				<span class="text-sm font-medium">Engineering</span>
			}
			@item.Content() {
				@item.Title() {
					API Documentation
				}
				@item.Description() {
					Complete reference guide for all available endpoints and authentication methods.
				}
			}
		}
	}
}

templ itemExampleFooterSection() {
	@itemExampleSection("ItemFooter") {
		@item.Item() {
			@item.Content() {
				@item.Title() {
					Quarterly Report Q4 2024
				}
				@item.Description() {
					Financial overview including revenue, expenses, and growth metrics for the fourth quarter.
				}
			}
			@item.Footer() {
				<span class="text-sm text-muted-foreground">Last updated 2 hours ago</span>
			}
		}
		@item.Item(item.Props{Variant: item.VariantOutline}) {
			@item.Content() {
				@item.Title() {
					User Research Findings
				}
				@item.Description() {
					Insights from interviews and surveys conducted with 50+ users across different demographics.
				}
			}
			@item.Footer() {
				<span class="text-sm text-muted-foreground">Created by Sarah Chen</span>
			}
		}
		@item.Item(item.Props{Variant: item.VariantMuted}) {
			@item.Content() {
				@item.Title() {
					Product Roadmap
				}
				@item.Description() {
					Planned features and improvements scheduled for the next three months.
				}
			}
			@item.Footer() {
				<span class="text-sm text-muted-foreground">12 comments</span>
			}
		}
	}
}

templ itemExampleHeaderAndFooterSection() {
	@itemExampleSection("ItemHeader + ItemFooter") {
		@item.Item() {
			@item.Header() {
				<span class="text-sm font-medium">Team Project</span>
			}
			@item.Content() {
				@item.Title() {
					Website Redesign
				}
				@item.Description() {
					Complete overhaul of the company website with modern design principles and improved user experience.
				}
			}
			@item.Footer() {
				<span class="text-sm text-muted-foreground">Updated 5 minutes ago</span>
			}
		}
		@item.Item(item.Props{Variant: item.VariantOutline}) {
			@item.Header() {
				<span class="text-sm font-medium">Client Work</span>
			}
			@item.Content() {
				@item.Title() {
					Mobile App Development
				}
				@item.Description() {
					Building a cross-platform mobile application for iOS and Android with React Native.
				}
			}
			@item.Footer() {
				<span class="text-sm text-muted-foreground">Status: In Progress</span>
			}
		}
		@item.Item(item.Props{Variant: item.VariantMuted}) {
			@item.Header() {
				<span class="text-sm font-medium">Documentation</span>
			}
			@item.Content() {
				@item.Title() {
					API Integration Guide
				}
				@item.Description() {
					Step-by-step instructions for integrating third-party APIs with authentication and error handling.
				}
			}
			@item.Footer() {
				<span class="text-sm text-muted-foreground">Category: Technical • 3 attachments</span>
			}
		}
	}
}

templ itemExampleImage(name string) {
	<img
		src={ "https://avatar.vercel.sh/" + name }
		alt={ name }
		width="40"
		height="40"
		class="object-cover grayscale"
	/>
}

// itemExampleImageSection is one of the ItemMedia image sections; the extra
// small one drops the descriptions like the tsx does.
templ itemExampleImageSection(title string, variant item.Variant, size item.Size, withDescriptions bool) {
	@itemExampleSection(title) {
		@item.Item(item.Props{Variant: variant, Size: size}) {
			@item.Media(item.MediaProps{Variant: item.MediaVariantImage}) {
				@itemExampleImage("Project")
			}
			@item.Content() {
				@item.Title() {
					Project Dashboard
				}
				if withDescriptions {
					@item.Description() {
						Overview of project settings and configuration.
					}
				}
			}
		}
		@item.Item(item.Props{Variant: variant, Size: size}) {
			@item.Media(item.MediaProps{Variant: item.MediaVariantImage}) {
				@itemExampleImage("Document")
			}
			@item.Content() {
				@item.Title() {
					Document
				}
				if withDescriptions {
					@item.Description() {
						A document with metadata displayed.
					}
				}
			}
			@item.Actions() {
				@button.Button(button.Props{Variant: button.VariantOutline, Size: button.SizeSm}) {
					View
				}
			}
		}
		@item.Item(item.Props{Variant: variant, Size: size}) {
			@item.Media(item.MediaProps{Variant: item.MediaVariantImage}) {
				@itemExampleImage("File")
			}
			@item.Content() {
				@item.Title() {
					File Attachment
				}
				if withDescriptions {
					@item.Description() {
						Complete file with image, title, description, and action button.
					}
				}
			}
			@item.Actions() {
				@button.Button(button.Props{Size: button.SizeSm}) {
					Download
				}
			}
		}
	}
}
