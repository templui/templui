package examples

import (
	"github.com/templui/templui/components/button"
	"github.com/templui/templui/components/dropdownmenu"
	"github.com/templui/templui/components/icon"
	"github.com/templui/templui/components/input"
	selectcomp "github.com/templui/templui/components/select"
	"github.com/templui/templui/components/table"
)

// The kitchen-sink page from table-example.tsx.
templ tableExampleSection(title string) {
	<div data-slot="example" class="mx-auto flex w-full max-w-lg min-w-0 flex-col gap-1 self-stretch lg:max-w-none">
		if title != "" {
			<div class="px-1.5 py-2 text-xs font-medium text-muted-foreground">{ title }</div>
		}
		<div data-slot="example-content" class="flex min-w-0 flex-1 flex-col items-start gap-6 rounded-xl bg-card p-12 text-foreground style-lyra:rounded-none style-sera:rounded-none *:[div:not([class*='w-'])]:w-full">
			{ children... }
		</div>
	</div>
}

templ TableExample() {
	<div class="w-full bg-muted dark:bg-background">
		<div data-slot="example-wrapper" class="mx-auto grid min-h-screen w-full max-w-5xl min-w-0 content-center items-start gap-8 p-4 pt-2 sm:gap-12 sm:p-6 md:grid-cols-2 md:gap-8 lg:p-12 2xl:max-w-6xl">
			@tableExampleBasic()
			@tableExampleWithFooter()
			@tableExampleSimple()
			@tableExampleWithBadges()
			@tableExampleWithActions()
			@tableExampleWithSelect()
			@tableExampleWithInput()
		</div>
	</div>
}

templ tableExampleBasic() {
	@tableExampleSection("Basic") {
		@table.Table() {
			@table.Caption() {
				A list of your recent invoices.
			}
			@table.Header() {
				@table.Row() {
					@table.Head(table.HeadProps{Class: "w-[100px]"}) {
						Invoice
					}
					@table.Head() {
						Status
					}
					@table.Head() {
						Method
					}
					@table.Head(table.HeadProps{Class: "text-right"}) {
						Amount
					}
				}
			}
			@table.Body() {
				for _, invoice := range tableInvoices[:3] {
					@table.Row() {
						@table.Cell(table.CellProps{Class: "font-medium"}) {
							{ invoice.Invoice }
						}
						@table.Cell() {
							{ invoice.PaymentStatus }
						}
						@table.Cell() {
							{ invoice.PaymentMethod }
						}
						@table.Cell(table.CellProps{Class: "text-right"}) {
							{ invoice.TotalAmount }
						}
					}
				}
			}
		}
	}
}

templ tableExampleWithFooter() {
	@tableExampleSection("With Footer") {
		@table.Table() {
			@table.Caption() {
				A list of your recent invoices.
			}
			@table.Header() {
				@table.Row() {
					@table.Head(table.HeadProps{Class: "w-[100px]"}) {
						Invoice
					}
					@table.Head() {
						Status
					}
					@table.Head() {
						Method
					}
					@table.Head(table.HeadProps{Class: "text-right"}) {
						Amount
					}
				}
			}
			@table.Body() {
				for _, invoice := range tableInvoices[:3] {
					@table.Row() {
						@table.Cell(table.CellProps{Class: "font-medium"}) {
							{ invoice.Invoice }
						}
						@table.Cell() {
							{ invoice.PaymentStatus }
						}
						@table.Cell() {
							{ invoice.PaymentMethod }
						}
						@table.Cell(table.CellProps{Class: "text-right"}) {
							{ invoice.TotalAmount }
						}
					}
				}
			}
			@table.Footer() {
				@table.Row() {
					@table.Cell(table.CellProps{Attributes: templ.Attributes{"colspan": "3"}}) {
						Total
					}
					@table.Cell(table.CellProps{Class: "text-right"}) {
						$2,500.00
					}
				}
			}
		}
	}
}

templ tableExampleSimple() {
	@tableExampleSection("Simple") {
		@table.Table() {
			@table.Header() {
				@table.Row() {
					@table.Head() {
						Name
					}
					@table.Head() {
						Email
					}
					@table.Head(table.HeadProps{Class: "text-right"}) {
						Role
					}
				}
			}
			@table.Body() {
				@table.Row() {
					@table.Cell(table.CellProps{Class: "font-medium"}) {
						Sarah Chen
					}
					@table.Cell() {
						sarah.chen@acme.com
					}
					@table.Cell(table.CellProps{Class: "text-right"}) {
						Admin
					}
				}
				@table.Row() {
					@table.Cell(table.CellProps{Class: "font-medium"}) {
						Marc Rodriguez
					}
					@table.Cell() {
						marcus.rodriguez@acme.com
					}
					@table.Cell(table.CellProps{Class: "text-right"}) {
						User
					}
				}
				@table.Row() {
					@table.Cell(table.CellProps{Class: "font-medium"}) {
						Emily Watson
					}
					@table.Cell() {
						emily.watson@acme.com
					}
					@table.Cell(table.CellProps{Class: "text-right"}) {
						User
					}
				}
			}
		}
	}
}

templ tableExampleWithBadges() {
	@tableExampleSection("With Badges") {
		@table.Table() {
			@table.Header() {
				@table.Row() {
					@table.Head() {
						Task
					}
					@table.Head() {
						Status
					}
					@table.Head(table.HeadProps{Class: "text-right"}) {
						Priority
					}
				}
			}
			@table.Body() {
				@table.Row() {
					@table.Cell(table.CellProps{Class: "font-medium"}) {
						Design homepage
					}
					@table.Cell() {
						<span class="inline-flex items-center rounded-full bg-green-500/10 px-2 py-1 text-xs font-medium text-green-700 dark:text-green-400">
							Completed
						</span>
					}
					@table.Cell(table.CellProps{Class: "text-right"}) {
						<span class="inline-flex items-center rounded-full bg-blue-500/10 px-2 py-1 text-xs font-medium text-blue-700 dark:text-blue-400">
							High
						</span>
					}
				}
				@table.Row() {
					@table.Cell(table.CellProps{Class: "font-medium"}) {
						Implement API
					}
					@table.Cell() {
						<span class="inline-flex items-center rounded-full bg-yellow-500/10 px-2 py-1 text-xs font-medium text-yellow-700 dark:text-yellow-400">
							In Progress
						</span>
					}
					@table.Cell(table.CellProps{Class: "text-right"}) {
						<span class="inline-flex items-center rounded-full bg-gray-500/10 px-2 py-1 text-xs font-medium text-gray-700 dark:text-gray-400">
							Medium
						</span>
					}
				}
				@table.Row() {
					@table.Cell(table.CellProps{Class: "font-medium"}) {
						Write tests
					}
					@table.Cell() {
						<span class="inline-flex items-center rounded-full bg-gray-500/10 px-2 py-1 text-xs font-medium text-gray-700 dark:text-gray-400">
							Pending
						</span>
					}
					@table.Cell(table.CellProps{Class: "text-right"}) {
						<span class="inline-flex items-center rounded-full bg-gray-500/10 px-2 py-1 text-xs font-medium text-gray-700 dark:text-gray-400">
							Low
						</span>
					}
				}
			}
		}
	}
}

type tableExampleProduct struct {
	Name  string
	Price string
}

var tableExampleProducts = []tableExampleProduct{
	{"Wireless Mouse", "$29.99"},
	{"Mechanical Keyboard", "$129.99"},
	{"USB-C Hub", "$49.99"},
}

templ tableExampleWithActions() {
	@tableExampleSection("With Actions") {
		@table.Table() {
			@table.Header() {
				@table.Row() {
					@table.Head() {
						Product
					}
					@table.Head() {
						Price
					}
					@table.Head(table.HeadProps{Class: "text-right"}) {
						Actions
					}
				}
			}
			@table.Body() {
				for _, product := range tableExampleProducts {
					@table.Row() {
						@table.Cell(table.CellProps{Class: "font-medium"}) {
							{ product.Name }
						}
						@table.Cell() {
							{ product.Price }
						}
						@table.Cell(table.CellProps{Class: "text-right"}) {
							@dropdownmenu.DropdownMenu() {
								@button.Button(button.Props{
									Variant:    button.VariantGhost,
									Size:       button.SizeIcon,
									Class:      "size-8",
									Attributes: dropdownmenu.Trigger(ctx),
								}) {
									@icon.Ellipsis()
									<span class="sr-only">Open menu</span>
								}
								@dropdownmenu.Content(dropdownmenu.ContentProps{Align: dropdownmenu.AlignEnd}) {
									@dropdownmenu.Item() {
										Edit
									}
									@dropdownmenu.Item() {
										Duplicate
									}
									@dropdownmenu.Separator()
									@dropdownmenu.Item(dropdownmenu.ItemProps{Variant: dropdownmenu.ItemVariantDestructive}) {
										Delete
									}
								}
							}
						}
					}
				}
			}
		}
	}
}

type tableExamplePerson struct {
	Value string
	Label string
}

var tableExamplePeople = []tableExamplePerson{
	{"sarah", "Sarah Chen"},
	{"marcus", "Marc Rodriguez"},
	{"emily", "Emily Watson"},
	{"david", "David Kim"},
}

type tableExampleTask struct {
	Task     string
	Assignee string
	Status   string
}

var tableExampleTasks = []tableExampleTask{
	{"Design homepage", "sarah", "In Progress"},
	{"Implement API", "marcus", "Pending"},
	{"Write tests", "emily", "Not Started"},
}

templ tableExampleWithSelect() {
	@tableExampleSection("With Select") {
		@table.Table() {
			@table.Header() {
				@table.Row() {
					@table.Head() {
						Task
					}
					@table.Head() {
						Assignee
					}
					@table.Head() {
						Status
					}
				}
			}
			@table.Body() {
				for _, task := range tableExampleTasks {
					@table.Row() {
						@table.Cell(table.CellProps{Class: "font-medium"}) {
							{ task.Task }
						}
						@table.Cell() {
							@selectcomp.Select(selectcomp.Props{Value: task.Assignee}) {
								@selectcomp.Trigger(selectcomp.TriggerProps{Class: "w-40", Size: selectcomp.SizeSm}) {
									@selectcomp.Value()
								}
								@selectcomp.Content() {
									@selectcomp.Group() {
										for _, person := range tableExamplePeople {
											@selectcomp.Item(selectcomp.ItemProps{Value: person.Value}) {
												{ person.Label }
											}
										}
									}
								}
							}
						}
						@table.Cell() {
							{ task.Status }
						}
					}
				}
			}
		}
	}
}

templ tableExampleWithInput() {
	@tableExampleSection("With Input") {
		@table.Table() {
			@table.Header() {
				@table.Row() {
					@table.Head() {
						Product
					}
					@table.Head() {
						Quantity
					}
					@table.Head() {
						Price
					}
				}
			}
			@table.Body() {
				@table.Row() {
					@table.Cell(table.CellProps{Class: "font-medium"}) {
						Wireless Mouse
					}
					@table.Cell() {
						@input.Input(input.Props{
							Type:       input.TypeNumber,
							Value:      "1",
							Class:      "h-8 w-20",
							Attributes: templ.Attributes{"min": "0"},
						})
					}
					@table.Cell() {
						$29.99
					}
				}
				@table.Row() {
					@table.Cell(table.CellProps{Class: "font-medium"}) {
						Mechanical Keyboard
					}
					@table.Cell() {
						@input.Input(input.Props{
							Type:       input.TypeNumber,
							Value:      "2",
							Class:      "h-8 w-20",
							Attributes: templ.Attributes{"min": "0"},
						})
					}
					@table.Cell() {
						$129.99
					}
				}
				@table.Row() {
					@table.Cell(table.CellProps{Class: "font-medium"}) {
						USB-C Hub
					}
					@table.Cell() {
						@input.Input(input.Props{
							Type:       input.TypeNumber,
							Value:      "1",
							Class:      "h-8 w-20",
							Attributes: templ.Attributes{"min": "0"},
						})
					}
					@table.Cell() {
						$49.99
					}
				}
			}
		}
	}
}
